[package]
name = "matinee-fixture"
version = "0.0.2"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
axum = "=0.8.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "sync"] }
