# Spec 006 QA Report

**Date**: 2026-09-19
**Feature**: 006-identity-authorization-secure-channels
**Mode**: CLI / Rust lifecycle smoke test
**Verdict**: PARTIAL PASS

## Evidence and environment

- Worktree: `omp/agent/fix-sec010-reset` at `cfb76309` before this phase.
- Driver: `cargo run -p matinee-runtime --features test-support --example enrollment_lifecycle`.
- Target isolation: `.qa-target` was proven absent with `stat` before execution; `CARGO_TARGET_DIR=.qa-target` was used.
- Captured output: `qa-lifecycle-output.txt` in this directory. Exit status was 0.
- The example output exercises create, authenticated key delivery, proof consumption, reconnect, custody rotation, revoke, and the intentional capability refusal.
- Existing review evidence covers the deterministic contract and fault matrices; this phase did not substitute the test suite for the built-artifact smoke test.

## Captured lifecycle result

The seven numbered steps completed as follows:

1. Pairing created with a one-time key fingerprint and loopback endpoint.
2. Authenticated native channel sealed the one-time key; projection exposed only ciphertext byte count.
3. Pairing proof consumed and a long-term principal registered.
4. Replacement connection reconnected on shared registry state.
5. Custody rotated and the stale fingerprint was refused.
6. Principal was terminally revoked.
7. A browser lacking required key semantics was refused with `CapabilityRejected`.

The final line was `enrollment lifecycle complete` and the process exited successfully.

## Acceptance scenario coverage

### User Story 1: bootstrap

- US1-1 clean bootstrap: PARTIAL. The lifecycle creates an enrollment rather than exercising inherited-OS-pipe first-principal bootstrap. Covered by `bootstrap_contract.rs` and `bootstrap_recovery.rs`.
- US1-2 staged crash retry convergence: COVERED by `bootstrap_recovery.rs` crash-before-commit cases.
- US1-3 post-commit recovery: COVERED by `bootstrap_recovery.rs` crash-after-commit cases.
- US1-4 different bootstrap identity fails without mutation: COVERED by `bootstrap_recovery.rs` mismatched/different identity cases.

### User Story 2: enrollment and reconnect

- US2-1 ten-minute, one-use, entropy, origin, daemon binding: OBSERVED in lifecycle step 1 and covered in `enrollment_contract.rs`.
- US2-2 valid proof creates distinct long-term principal atomically: OBSERVED in steps 2-3; covered in `enrollment_contract.rs` and `enrollment_custody.rs`.
- US2-3 consumed, expired, revoked, wrong-origin, wrong-identity refusal before payload: OBSERVED for revoked/stale capability in steps 5-7; consumed/expired/origin/identity variants covered by `enrollment_failures.rs`.
- US2-4 reconnect resumes authorized identity: OBSERVED in step 4; covered by `enrollment_custody.rs`.

### User Story 3: authenticated encrypted channel

- US3-1 mutual transcript and directional keys before payload: PARTIAL in step 2 (authenticated encrypted delivery); handshake/vector proof covered by `secure_channel_handshake.rs` and `secure_channel_frames.rs`.
- US3-2 disjoint/tampered contracts fail before mutation: covered by `secure_channel_faults.rs`.
- US3-3 observer/relay alteration cannot recover or modify payload: covered by secure-channel vectors and fault tests; no standalone observer exists in the lifecycle example.
- US3-4 repeated/skipped/wrapped/wrong-direction counters close without dispatch: covered by `secure_channel_faults.rs`.

### User Story 4: authorization and privacy

- US4-1 owned MCP resource returns bounded result: covered by `authorization_contract.rs` and `authorization_privacy.rs`.
- US4-2 cross-owner/admin-only request maps to indistinguishable not-found: covered by `authorization_privacy.rs`.
- US4-3 extension outside identity/grant is denied without leak: covered by `authorization_privacy.rs`.
- US4-4 malformed/unauthorized request does not mutate and emits redacted event: covered by `authorization_privacy.rs` and `events_contract.rs`.

### User Story 5: rotation and revocation

- US5-1 rotation registers replacement, advances epoch, closes old channels: OBSERVED in step 5; covered by `rotation_revocation.rs`.
- US5-2 revoked frame/reconnect fails before disclosure or mutation: OBSERVED in step 6 and stale refusal in step 5; covered by `rotation_revocation_races.rs`.
- US5-3 revoked extension decisions invalidate: covered by `rotation_revocation_races.rs`.
- US5-4 repeated transition is idempotent: covered by `rotation_revocation.rs` and `rotation_revocation_races.rs`.

## Scenarios not exercised by the built-artifact lifecycle

The example does not exercise first-principal inherited-pipe bootstrap, crash injection, observer/relay transport tampering, authorization matrix routes, malformed frame corpus, or event sink aggregation. Those scenarios are covered by the named contract/fault tests above. No acceptance scenario is entirely unexercised by either the lifecycle driver or the existing contract evidence.

## Quickstart verification

`quickstart.md` was checked against the current exports and fixture paths. No documented command is stale: the lifecycle command is present and ran successfully, and the documented Node/Chrome fixture paths and focused Rust test paths exist. No documented Rust function signature is given as an invocation that no longer exists. The current enrollment constructor requires a trusted creation instant (`EnrollmentBundle::create(input, created_ms)`), and the quickstart correctly describes the instant as host-supplied clock context rather than claiming a caller-controlled instant in the enrollment description.

## Suite context

The prior formal review recorded 427 default tests and 435 all-features tests, both passing. This phase also ran focused `matinee-security` verification after the cleanup edit: 233 tests passed in default mode and 233 passed with all features. No failure was introduced.

## Limitations and findings

The lifecycle smoke driver covers the critical pairing flow but not every acceptance matrix. The contract and fault tests supply the remaining evidence. Step 7 records the required fail-closed `CapabilityRejected` outcome.
