# Spec 006 Cleanup Report

**Date**: 2026-09-19
**Scope**: post-implementation cleanup gate for Spec 006
**Verdict**: PASS with recorded non-blocking findings

## Quality gate output

- The shared SpecKit prerequisite script completed and identified the Spec 006 feature directory and tasks ledger.
- `cargo fmt --all -- --check`: PASS.
- Focused default `cargo test -p matinee-security`: 233 passed, 0 failed.
- Focused all-features `cargo test -p matinee-security --all-features`: 233 passed, 0 failed.
- The previously measured full workspace gate remains 427 default tests and 435 all-features tests, with 0 failures. The final full gate was rerun after the cleanup edit with a private, absent target directory and touched `crates/matinee-security/src/lib.rs`; those counts remained 427 and 435.
- No formatter, linter, or test failure was introduced by cleanup.

## Scout-rule fix

The cleanup gate found one small issue in `crates/matinee-runtime/examples/enrollment_lifecycle.rs`: two unused imports (`EnrollmentConsumeError` and `EnrollmentFailure`) caused a compiler warning. The edit removed them. This change only improves example hygiene and leaves production security logic unchanged.

## Medium findings recorded, not fixed

These findings require design or security-owner judgment and were intentionally not changed in this phase:

1. Several host-budget read-only closures are identical and could be extracted into one helper.
2. The 60-second and ten-failure policy is distributed across multiple call sites plus `before_attempt` and `record_failure`, without one shared decision token. Consolidation would change security-sensitive structure and is not a cleanup edit.
3. The defensive `record_failure == RateLimited` match arm is unreachable under the current invariant. Converting it to an assertion needs an explicit policy decision.
4. Compiler dead-code warnings remain for private adapter paths and command helpers because downstream integrations are intentionally out of Spec 006 scope. Removing them would alter the private boundary or remove planned seams.
5. Low/Tier-3 residuals from the completed security review remain: enrollment-key zeroization, public principal construction and registry insertion, raw clock provenance at public creation/clock constructors, caller-claimed capability/owner in `receive`/`send_projection`, and `sign_sealed_for_test` being available in non-test all-features builds. Both crates are `publish = false`; these are tracked residuals, not cleanup regressions.

No new task was appended because Beads writes are unavailable and the assignment explicitly requires recording findings rather than writing beads or changing the implementation ledger.

## Repair-debris sweep

- No `reset_for_test` symbol remains.
- No TODO, FIXME, HACK, or XXX marker introduced during the repair rounds was found in the scoped implementation.
- No commented-out code block introduced during the repair rounds was found.
- The `println!` calls in test evidence files are intentional campaign evidence, not debugging remnants.
- Comments describing the creation instant are current: they explicitly say the trusted host/transition supplies the instant and that the enrollment description cannot supply a second caller-controlled instant. No stale comment claiming a caller-supplied instant was found.
- No leftover scaffolding or placeholder implementation was found in the scoped crate.
- The only cleanup source edit was the unused-import removal above.

## Scope and safety confirmation

Production security logic was not changed. No Beads record was written. Nothing was pushed. The cleanup artifacts and lifecycle output are the only new phase evidence, plus the warning-only example import cleanup.
