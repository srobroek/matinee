---
document_type: speckit-analysis
feature: 006-identity-authorization-secure-channels
analysis_date: 2026-09-19
head: a9731080
---

# Specification Analysis Report

## Scope and method

The prerequisite script succeeded when given `SPECIFY_FEATURE_DIRECTORY=specs/006-identity-authorization-secure-channels`. The analyzed artifacts were `spec.md`, `plan.md`, `tasks.md`, the four files under `contracts/`, `quickstart.md`, and the constitution. The analysis is post-implementation: task checkboxes were reconciled against source paths and asserting tests, rather than treated as historical implementation truth.

No executable `before_analyze` or `after_analyze` hook is registered in `.specify/extensions.yml`; its hooks are for other phases and are optional.

## Findings

| ID | Category | Severity | Location(s) | Summary | Recommendation |
|---|---|---|---|---|---|
| I1 | Inconsistency | HIGH | `plan.md:18-35,105-107,206-210`; `crates/matinee-security/Cargo.toml:10-15` | The plan says the fixed direct dependency set excludes a second cryptographic library and says the existing runtime remains unchanged, while the implementation uses direct `p256` and `libc` dependencies and adds runtime enrollment integration. | Treat the current implementation and the accepted security review as the authority for the deliberate scope. Record the dependency and runtime ownership divergence in the next plan revision or decision record. This phase records the issue and does not change production code. |
| I2 | Identifier drift | HIGH | `tasks.md:18-20`; `security-review-branch-2026-09-19.md:56,68,77,86,95` | The task ledger requires `adr-5` and `adr-6`, but the reviewed repository records accepted ADRs 0006 and 0007. The security artifact names `TASK-SEC-001` through `TASK-SEC-004`, while `tasks.md` defines no `TASK-SEC-*` rows. | Add explicit trace links in a future artifact update. Do not fabricate Beads IDs or rewrite the historical review. |
| I3 | Identifier drift | MEDIUM | `tasks.md:33-35,44`; reviewer handoff references to T007 and T019H | Review shorthand calls 'T007 default expiry semantics' and 'T019H contract harness', but T007 is the public-boundary module task and there is no T019H. Default expiry is implemented under the enrollment task path (T032) and the malformed corpus harness is T018. | Use the task IDs in `tasks.md` as canonical and retain the shorthand only as a historical alias in analysis notes. |
| I4 | Acceptance divergence | MEDIUM | `tasks.md:90-94`; `src/enrollment.rs:26-31,138-176,1694-1762`; `tests/enrollment_contract.rs`, `tests/enrollment_failures.rs` | T032/T035 acceptance text describes the original ten-minute/default and budget shape. The implementation bounds a named absolute deadline as well as the ten-minute default, and the host-budget implementation includes an unattributable bucket path. Tests assert these repaired semantics. | Keep the tasks checked, but preserve this divergence in the reconciliation record; do not silently restate the old acceptance as the only behavior. |
| I5 | Acceptance divergence | MEDIUM | `tasks.md:169-171`; `quickstart.md:323-347`; `tests/quickstart_evidence.rs:72-78,1490-1548` | T056/T058 ask for complete quickstart and peer evidence. The recorded evidence intentionally reports the Chrome capability fixture as unsupported under Node and reports zero tests in the standalone integration target because the tests are included by the library target. | Treat explicit unsupported and zero-test results as evidence, not as successful browser lifecycle proof. The real Chrome lifecycle remains downstream Spec 008 and the runtime test discovery defect remains known `matinee-33l`. |
| I6 | Coverage gap | MEDIUM | `contracts/failures-events.md:113-128`; `contracts/authorization.md:83-89`; `contracts/enrollment-bootstrap.md:113-119`; `plan.md:109-116` | Several contract clauses are intentionally unreferenced by a Spec 006 implementation task: durable event schema, digest chaining, retention, quotas, read/export authorization, and typed Spec 007/009 producers. | Keep these clauses explicitly marked downstream-owned. They are not missing implementation tasks in this scoped crate. |
| I7 | Coverage gap | LOW | `tasks.md:18-20,170`; `plan.md:118-120`; constitution `:118-120` | ADR creation/acceptance and documentation review are governance or prose tasks, not FR/SC implementation tasks, so they have no direct requirement row. | Keep T001-T003 blocked because this run is forbidden from writing Beads; retain T057 as a documentation gate. |
| I8 | Production inconsistency | MEDIUM | `plan.md:18-35`; `crates/matinee-security/Cargo.toml`; `src/identity.rs`; `src/adapters/os_pipe.rs` | The production dependency boundary in the plan says ring is the sole cryptographic library, but production public-key validation uses `p256`; `libc` is also absent from the plan's dependency rationale. This is the surviving SEC-001 issue from the earlier review, not a new finding. | Report to the lead for the dependency decision. No production source was changed in this phase. |
| I9 | Production inconsistency | LOW | `src/enrollment.rs:270-276,310-353,413-438,453-483`; runtime enrollment host | One-time PKCS#8 and wrapping-key buffers are not zeroized and pending bundles have longer process lifetime than the task acceptance implies. This is the established SEC-002 residual. | Track as hardening before external pairing routes; do not duplicate the security review. |
| I10 | Production inconsistency | LOW | `src/identity.rs`; `src/transition.rs`; `src/channel.rs` | Public principal construction/activation and registry insertion leave a caller-built-principal integration hazard, despite whole-snapshot admission checks. This is the established SEC-003 residual. | Track as integration hardening before an untrusted route is exposed. |

## Requirement and success-criterion coverage

All 32 functional requirements and all 9 buildable success criteria have at least one task trace. T056/T064 provide the explicit FR-002--FR-032 and SC-001--SC-009 mapping, while T008-T055 provide the implementation and asserting-test paths. FR-001 is covered by the identity/public-boundary tasks (T007-T008) and the quickstart mapping; SC-001--SC-009 are covered by T028, T043, T048, T054, T056, and T064.

| Requirement inventory | Has task? | Task traces | Notes |
|---|---|---|---|
| FR-001--FR-009 | Yes | T007-T036, T056, T059-T060 | Bootstrap and enrollment paths; T007 is module boundary, not expiry. |
| FR-010--FR-019 | Yes | T013-T016, T037-T043, T056, T061 | Native/WebCrypto vectors and channel tests; Chrome support is fixture-level and downstream-owned for lifecycle. |
| FR-020--FR-023 | Yes | T044-T048, T062 | Authorization matrix, privacy, and pre-serialization filtering. |
| FR-024--FR-032 | Yes | T019-T020, T049-T058, T063-T064 | Transition, event, redaction, malformed, and evidence paths. |
| SC-001--SC-002 | Yes | T028, T059 | Bootstrap recovery and secret-custody tests. |
| SC-003, SC-007 | Yes | T037-T043, T061, T018, T064 | Vector/fault tests and malformed corpus. |
| SC-004--SC-006, SC-008--SC-009 | Yes | T048, T054-T056, T062-T064 | Authorization, transition races, events, and evidence mapping. |
| SC-005 | Yes | T029-T036, T060, T064 | Enrollment negative cases and budgets. |

## Unmapped tasks

- T001-T003 map to constitution governance and ADR prerequisites, not an FR/SC. They remain unchecked because this run cannot write Beads and the task text explicitly forbids claiming completion when ADR delivery is blocked.
- T057 maps to the documentation quality gate and is intentionally not a functional requirement. It is checked because the reviewed public-boundary comments and quickstart record match the implemented ownership boundary.
- T004-T006 map to plan architecture and the constitution's dependency gate rather than a single FR/SC; they are checked against the workspace manifest, crate manifest, and generated lockfile.

## Task reconciliation evidence

Every checked task has an implementing path and an asserting test or recorded command. Ranges below expand to each listed task ID; a range is used only where every task in the range has the same evidence shape.

| Checked tasks | Implementing or artifact path | Asserting test or evidence |
|---|---|---|
| T004-T006 | `Cargo.toml`, `crates/matinee-security/Cargo.toml`, `Cargo.lock` | Workspace and manifest inspection; recorded cargo verification. |
| T007-T012 | `src/lib.rs`, `src/identity.rs`, `src/failures.rs`, `src/events.rs`, `src/adapters/*`, `tests/support/fakes.rs` | `tests/foundation_contract.rs`; public-boundary and adapter tests. |
| T013-T016 | `vectors/README.txt`, `vectors/secure-channel-v1.json`, both fixture directories | Node WebCrypto and Chrome capability commands recorded in `quickstart.md:327-333`. |
| T017-T020 | `tests/foundation_contract.rs`, `tests/malformed_corpus.rs`, `tests/events_contract.rs`, `src/events.rs` | Foundation, 100,000-case malformed, event-bound, redaction, aggregation, and sink-unavailable tests. |
| T021-T028 | bootstrap tests plus `src/adapters/*`, `src/identity.rs`, `src/transition.rs` | `bootstrap_contract.rs`, `bootstrap_recovery.rs`, including crash, retry, credential, and secret scans. |
| T029-T036 | `tests/enrollment_contract.rs`, `enrollment_failures.rs`, `enrollment_custody.rs`, `src/enrollment.rs` | Enrollment creation, negative/race, custody, budget, endpoint, metadata, expiry, and reconnect tests. |
| T037-T043 | `src/channel.rs`, `src/lib.rs`, secure-channel tests, vector fixtures | Handshake, frame, fault, native vector, and Node WebCrypto evidence. |
| T044-T048 | `src/authorization.rs`, `src/channel.rs`, authorization tests | Authorization matrix, privacy, filtering, denial, event, and no-dispatch tests. |
| T049-T054 | `src/transition.rs`, `src/channel.rs`, rotation/revocation tests | Idempotency, race, stale epoch, disconnect, invalidation, uncertainty, and event-gate tests. |
| T055-T058 | `tests/events_contract.rs`, `tests/quickstart_evidence.rs`, `quickstart.md`, `src/lib.rs` | Event aggregation/redaction tests, FR/SC mapping test, fixture commands, and recorded focused evidence. |
| T059-T060 | adapters, `src/transition.rs`, `src/enrollment.rs`, bootstrap/enrollment tests | Same evidence as T024-T036, now explicitly reconciled as convergence work. |
| T061-T063 | channel, authorization, transition, and corresponding tests | Same evidence as T037-T054, now explicitly reconciled as convergence work. |
| T064 | `tests/quickstart_evidence.rs`, `quickstart.md` | Quickstart commands and recorded evidence at `quickstart.md:323-347`. |

T001-T003 remain unchecked. No SEC task row is added because the authoritative security artifacts use finding IDs rather than implementation tasks; SEC-002 and SEC-003 are retained residuals, while the dependency finding and prior identifiers remain historical review data.

## Metrics

- Total requirements: 41 buildable identifiers (FR-001--FR-032 and SC-001--SC-009).
- Total task IDs: 64.
- Checked tasks: 61; unchecked tasks: 3 (T001-T003).
- Requirement coverage: 100% (41/41 have at least one task trace).
- Ambiguity or identifier-drift findings: 3.
- Duplication findings: 0.
- Critical findings: 0.
- Production inconsistencies reported: 3 established residuals (I8-I10), none fixed here.

## Next actions

1. The lead should decide how to resolve or explicitly accept the `p256` and `libc` dependency divergence before publishing a merge-ready security decision.
2. Preserve T001-T003 as blocked until the ADR references are reconciled through the repository decision workflow.
3. Keep SEC-002 and SEC-003 as tracked hardening work and preserve downstream ownership for Specs 007, 008, and 015.
