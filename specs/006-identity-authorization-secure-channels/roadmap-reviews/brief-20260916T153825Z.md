# Pre-Implementation Brief Report: Identity, Authorization, and Secure Channels

**Date**: 2026-09-16
**Feature**: [Spec 006 -- Identity, Authorization, and Secure Channels](../spec.md)
**Roadmap**: [Spec roadmap](../../../.specify/memory/roadmap.md), entry 006
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Summary

This pre-implementation brief compares Spec 006 with roadmap entry 006, the constitution, and the normative Spec 001 contracts. The roadmap entry is an unambiguous match by title and `specs/006-identity-authorization-secure-channels/` feature directory. The dependency, Spec 005 Runtime Foundation, is **verified** in the roadmap (the roadmap records verification after PR #5), so the dependency gate is satisfied. Spec 006 preserves the recorded identity, authorization, enrollment, secure-channel, origin, rotation, and revocation outcome and scope, and its requirements directly carry forward the Spec 001 inherited-pipe bootstrap, P-256/channel profile, object-privacy, and connection-lifetime contracts. One non-blocking metadata discrepancy remains: the roadmap calls Spec 005 `verified`, while Spec 005's own front matter still says `Implemented`. No roadmap or source artifact was changed, the status transition remains a recommendation only.

---

## Surfaced Context *(brief: the roadmap entry's recorded intent)*

- **Entry**: 006 -- Identity, Authorization, and Secure Channels  ·  **Status**: planned
- **Description**: Implement local principal identity, authorization, enrollment, revocation, and authenticated communication between Matinee processes.
- **Outcome (expected)**: Approved principals connect through the specified channels. Unknown, revoked, downgraded, or replayed peers fail before mutation.
- **Scope (in / out)**: **In** -- credential-store integration, principal roles, ECDSA identity, key agreement, channel framing, origin checks, rotation, and revocation. **Out** -- browser operations and human approval policy.
- **Depends on**: 005 Runtime Foundation (**verified in roadmap**, Spec 005 front matter says `Implemented`, which is a metadata discrepancy, not an unsatisfied dependency).
- **Governed by**: C-01 Local ownership, C-02 Published MCP surface, C-03 Extension transport, C-06 Explicit authority, C-09 Deep module ownership. The configured ADR directory is absent (`adr_present: false`), and no ADR links are present in this entry, these constraints are therefore surfaced from the roadmap/constitution rather than resolved ADR files.
- **Addresses**: No PRD pointer recorded.
- **Related open questions / cross-cutting notes**: Roadmap open questions: **None**. Relevant notes: connection lifetime must remain separate from daemon-owned request lifetime, browser profile data remains browser-owned, Journey 1 (install, pair, connect) gates specs 005--009 and 016. Spec 001 is the normative umbrella and its contracts define the daemon/extension channel, identity, authorization, and pairing profile.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 💡 | status-drift | The roadmap records dependency 005 as `verified`, while `specs/005-runtime-foundation/spec.md` retains `Status: Implemented`. The roadmap's version 1.2.1 amendment and merge evidence support the dependency being satisfied, so this does not contradict Spec 006's technical prerequisites, but the metadata can confuse dependency audits. | Reconcile Spec 005's status metadata with the authoritative roadmap when that artifact is next updated, do not block Spec 006 planning on this discrepancy. |

### Drift assessment

- **Outcome**: No drift found. Spec 006 explicitly requires approved principals and authenticated encrypted channels, with fail-before-mutation behavior for revoked, stale, downgraded, replayed, malformed, and unauthorized peers.
- **Scope**: No drift found. The specification includes the roadmap's credential, identity, enrollment, framing, origin, rotation, and revocation work and explicitly excludes browser operations, human approval policy, daemon lifecycle/store ownership, and clock implementation.
- **Governance and prior contracts**: No drift found. Spec 006 carries C-01/C-02/C-03/C-06/C-09 and explicitly adopts Spec 001's inherited OS-pipe bootstrap, ECDSA P-256 identity, P-256 ephemeral agreement, HKDF-SHA-256, AES-256-GCM, transcript binding, authenticated counters, origin checks, role ceilings, object-not-found privacy, and connection/request separation.

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 1 |
| 🤔 Question | 0 |

## Recommended Actions *(proposed only -- never auto-applied)*

1. **F1**: Treat dependency 005 as satisfied for planning based on the roadmap's `verified` status and recorded merge evidence, reconcile the stale `Implemented` front matter in Spec 005 during a later roadmap/spec metadata maintenance pass.
2. **Status transition**: After this brief is accepted and planning is ready, apply the roadmap entry transition from `planned` to `in-progress` via `/speckit.roadmap.write`, do not apply it in this report.
3. **Proceed to planning**: Preserve the explicit Spec 001 contract profile and the stated out-of-scope boundaries in the plan and subsequent implementation slices.

---

**Severity Legend**: 🎯 Must-Address (blocks proceeding) · 💡 Recommendation (strongly suggested) · 🤔 Question (needs a decision). **This report is read-only**, apply any roadmap changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.brief`.*
